# Onde SurfBoard - Environment Variables
# Copy this to .env.local and fill in the values

# ============================================
# NextAuth Configuration
# ============================================

# Generate a secret with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here

# Also needs this for next-auth v4
NEXTAUTH_SECRET=your-auth-secret-here

# Production URL (where the app is deployed)
NEXTAUTH_URL=https://onde.surf

# ============================================
# Google OAuth
# ============================================
# 1. Go to: https://console.cloud.google.com/apis/credentials
# 2. Create new OAuth 2.0 Client ID
# 3. Add authorized redirect URIs:
#    - http://localhost:3000/api/auth/callback/google (development)
#    - https://onde.surf/api/auth/callback/google (production)

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# CORDE API (local network image generation)
# ============================================
# The CORDE server runs on the local Mac Mini
# This should be the LAN IP address

NEXT_PUBLIC_CORDE_API=http://192.168.1.234:3700
